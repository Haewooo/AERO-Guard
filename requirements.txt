# Exact pins so a local install matches the versions in the shipped image.
# The image itself installs from requirements-full-<arch>.lock, which adds
# SHA-256 hashes for every wheel — regenerate both together with
# ./scripts/lock-requirements.sh.
fastapi==0.140.0
uvicorn[standard]==0.51.0
pydantic==2.13.4
pydantic-settings==2.14.2
